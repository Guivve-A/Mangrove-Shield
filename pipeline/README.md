# Pipeline de datos

Este módulo incluye utilidades para recalcular `priority_score` a partir de variables de riesgo.

Ejemplo:

```bash
python pipeline/generate_priority_zones.py --input data/demo/priority_zones.geojson --output data/demo/priority_zones.geojson
```
